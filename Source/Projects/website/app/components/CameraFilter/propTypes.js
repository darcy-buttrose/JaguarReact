import PropTypes from 'prop-types';


const cameraFilterPropTypes = {
  id: PropTypes.number,
  name: PropTypes.string,
  total_num_cameras: PropTypes.number,
  view_num_cameras: PropTypes.number,
};


export default cameraFilterPropTypes;
